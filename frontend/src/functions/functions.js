export const getAvailableScreenHeight = () => {
  const screenHeight = window.innerHeight;

  return screenHeight
}

export const getAvailableScreenWidth = () => {
  const screenWidth = window.innerWidth;

  return screenWidth
}